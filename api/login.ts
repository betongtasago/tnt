import {createClient}from'@supabase/supabase-js';
function secret(){return process.env.AUTH_SECRET||'tnt-development-secret-change-me';}
function checksum(value:string){let h=2166136261;for(let i=0;i<value.length;i++){h^=value.charCodeAt(i);h=Math.imul(h,16777619);}return(h>>>0).toString(36);}
function hashPassword(password:string){return checksum(`${password}.${secret()}`);}
function issueToken(username:string){const body=Buffer.from(JSON.stringify({u:username,exp:Date.now()+30*86400000})).toString('base64url');return `${body}.${checksum(`${body}.${secret()}`)}`;}
function validLogin(username:string,password:string,storedHash:string){const expected=storedHash||hashPassword(process.env.ADMIN_PASSWORD||'CHANGE_ME_NOW');return username==='admin'&&hashPassword(password)===expected;}
function db(){const url=String(process.env.SUPABASE_URL||'').trim(),key=String(process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SECRET_KEY||'').trim();if(!url||!key)return null;return createClient(url,key,{auth:{autoRefreshToken:false,persistSession:false}});}
export default async function handler(req:any,res:any){if(req.method!=='POST')return res.status(405).json({message:'Method Not Allowed'});try{const raw=typeof req.body==='string'?req.body:(req.body||{});const body=typeof raw==='string'?JSON.parse(raw||'{}'):raw;const username=String(body?.username||'').trim();const password=String(body?.password||'');let stored='';const client=db();if(client){const result=await client.from('app_state').select('admin_password_hash').eq('id','default').maybeSingle();if(!result.error)stored=String(result.data?.admin_password_hash||'');}if(!validLogin(username,password,stored))return res.status(401).json({message:'Sai tài khoản hoặc mật khẩu.'});return res.status(200).json({token:issueToken('admin'),user:{username:'admin',role:'admin',displayName:'Quản trị viên Tasago'}});}catch(error){console.error('login_error',error);return res.status(400).json({message:'Dữ liệu đăng nhập không hợp lệ.'});}}
