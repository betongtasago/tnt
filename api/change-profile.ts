import {createClient}from'@supabase/supabase-js';
function secret(){return process.env.AUTH_SECRET||'tnt-development-secret-change-me';}function checksum(value:string){let h=2166136261;for(let i=0;i<value.length;i++){h^=value.charCodeAt(i);h=Math.imul(h,16777619);}return(h>>>0).toString(36);}function readUser(req:any){const raw=String(req.headers?.authorization||'').replace(/^Bearer\s+/,'');const [body,sig]=raw.split('.');if(!body||!sig||sig!==checksum(`${body}.${secret()}`))return null;try{const p=JSON.parse(Buffer.from(body,'base64url').toString());return p.exp>Date.now()?{username:String(p.u),role:p.r==='admin'?'admin':'member'}:null;}catch{return null;}}
function db(){const url=String(process.env.SUPABASE_URL||'').trim(),key=String(process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SECRET_KEY||'').trim();if(!url||!key)throw Error('Thiếu cấu hình Supabase server-side.');return createClient(url,key,{auth:{autoRefreshToken:false,persistSession:false}});}
export default async function handler(req:any,res:any){if(req.method!=='POST')return res.status(405).json({message:'Method Not Allowed'});const user=readUser(req);if(!user)return res.status(401).json({message:'Phiên đăng nhập không hợp lệ.'});try{const b=typeof req.body==='string'?JSON.parse(req.body||'{}'):req.body||{};const displayName=String(b.displayName||'').trim();if(displayName.length<2||displayName.length>80)return res.status(400).json({message:'Tên hiển thị phải từ 2 đến 80 ký tự.'});const client=db();if(user.role==='member'){const{error}=await client.from('app_users').update({display_name:displayName,updated_at:new Date().toISOString()}).eq('username',user.username).eq('active',true);if(error)throw error;}else{const{error}=await client.from('app_state').update({admin_display_name:displayName,updated_at:new Date().toISOString()}).eq('id','default');if(error)throw error;}return res.json({success:true,user:{username:user.username,role:user.role,displayName},message:'Đã cập nhật tên hiển thị.'});}catch(e:any){console.error('change_profile_error',e);return res.status(500).json({message:e.message||'Không thể đổi tên hiển thị.'});}}
