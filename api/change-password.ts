import {createClient}from'@supabase/supabase-js';
function secret(){return process.env.AUTH_SECRET||'tnt-development-secret-change-me';}
function checksum(value:string){let h=2166136261;for(let i=0;i<value.length;i++){h^=value.charCodeAt(i);h=Math.imul(h,16777619);}return(h>>>0).toString(36);}
function readUser(req:any){const raw=String(req.headers?.authorization||'').replace(/^Bearer\s+/,'');const [body,sig]=raw.split('.');if(!body||!sig||sig!==checksum(`${body}.${secret()}`))return null;try{const p=JSON.parse(Buffer.from(body,'base64url').toString());return p.exp>Date.now()?p:null;}catch{return null;}}
function hashPassword(password:string){return checksum(`${password}.${secret()}`);}
function db(){const url=String(process.env.SUPABASE_URL||'').trim(),key=String(process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SECRET_KEY||'').trim();if(!url||!key)throw Error('Thiếu cấu hình Supabase server-side.');return createClient(url,key,{auth:{autoRefreshToken:false,persistSession:false}});}
export default async function handler(req:any,res:any){if(req.method!=='POST')return res.status(405).json({message:'Method Not Allowed'});if(!readUser(req))return res.status(401).json({message:'Phiên đăng nhập không hợp lệ.'});try{const b=typeof req.body==='string'?JSON.parse(req.body||'{}'):req.body||{};const current=String(b.currentPassword||''),next=String(b.newPassword||'');if(next.length<8)return res.status(400).json({message:'Mật khẩu mới phải có ít nhất 8 ký tự.'});const client=db();const{data,error}=await client.from('app_state').select('admin_password_hash').eq('id','default').maybeSingle();if(error)throw error;const stored=String(data?.admin_password_hash||'');const fallback=process.env.ADMIN_PASSWORD||'CHANGE_ME_NOW';if((stored&&hashPassword(current)!==stored)||(!stored&&current!==fallback))return res.status(400).json({message:'Mật khẩu hiện tại không đúng.'});const{error:updateError}=await client.from('app_state').update({admin_password_hash:hashPassword(next),updated_at:new Date().toISOString()}).eq('id','default');if(updateError)throw updateError;return res.json({success:true,message:'Đổi mật khẩu thành công.'});}catch(e:any){console.error(e);return res.status(500).json({message:e.message||'Không thể đổi mật khẩu.'});}}
